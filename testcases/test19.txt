const x = 7, y = 8;
var a, b, c;
begin
	a := x * y * 12;
	b := -(a - x);
	c := + a + b + var + y;
	if odd c then
		b := -b;
	while c > x do
		c := c - y;
	read b;
	write c;
end.


/* the preceding factor cannot begin with this symbol */
